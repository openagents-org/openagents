# OpenAgents Workspace Backend
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
python-dotenv>=1.0.0
firebase-admin>=6.0.0
# Sign in with Apple identity-token verification (RS256 + Apple JWKS).
# firebase-admin already pulls this in transitively; pinned explicitly so the
# Apple auth path doesn't silently break if that transitive dep changes.
pyjwt[crypto]>=2.8.0
aioapns>=3.4.0
boto3>=1.34.0
python-multipart>=0.0.9
httpx>=0.27.0
playwright>=1.40.0
anthropic>=0.39.0
openai>=1.0.0
redis>=5.0.0

# ── SWE-bench evaluation (OPTIONAL, EXPERIMENTAL, default-off) ──────────────
# Only needed when SWEBENCH_ENABLED=true. Imported lazily so the backend boots
# without them. Pinned, reproducible versions live in requirements-swebench.txt;
# install them on a Docker-capable host:
#   pip install -r requirements-swebench.txt
# (swebench==4.1.0, datasets==3.6.0, docker==7.1.0)
