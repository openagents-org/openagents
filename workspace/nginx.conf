upstream backend {
    server backend:8000;
}

upstream frontend {
    server frontend:3000;
}

server {
    listen 80;

    # Match the backend's MAX_FILE_SIZE (50MB). nginx defaults to 1m, which
    # 413s image uploads before they ever reach the backend.
    client_max_body_size 50m;

    # Redirect root path to main site (SEO: avoid brand keyword cannibalization)
    location = / {
        return 301 https://openagents.org/workspace;
    }

    # API requests → backend
    location /v1/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Everything else → frontend
    location / {
        proxy_pass http://frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
